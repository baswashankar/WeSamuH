<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>We Samuh | Natural Farm Products Marketplace</title>
    <meta name="description" content="Buy fresh, organic and natural farm products such as honey, mushrooms, milk, and kusuma oil directly from local farmers.">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- React & ReactDOM -->
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <!-- Babel -->
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <link rel="stylesheet" href="style.css">
    <style>
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&family=Playfair+Display:ital,wght@0,600;1,600&display=swap');

:root {
  --primary: #f59e0b; /* Honey yellow */
  --primary-light: #fcd34d;
  --primary-dark: #d97706;
  --secondary: #16a34a; /* Nature Green */
  --secondary-light: #4ade80;
  --secondary-dark: #15803d;
  --accent: #78350f; /* Deep Brown */
  --text-dark: #291507; 
  --text-light: #7c2d12;
  --text-muted: #6b7280;
  --bg-color: #fdfbf7;
  --bg-card: #ffffff;
  --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  --shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.05), 0 8px 10px -6px rgba(0, 0, 0, 0.01);
  --shadow-hover: 0 25px 50px -12px rgba(120, 53, 15, 0.15);
  --radius: 16px;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Outfit', sans-serif;
  background-color: var(--bg-color);
  color: var(--text-dark);
  line-height: 1.6;
  overflow-x: hidden;
}

h1, h2, h3, h4, h5, h6 {
  font-family: 'Playfair Display', serif;
  color: var(--accent);
  margin-bottom: 1rem;
}

a {
  text-decoration: none;
  color: inherit;
}

ul {
  list-style: none;
}

.title-large {
  font-size: clamp(3rem, 5vw, 4.5rem);
  line-height: 1.1;
  font-weight: 800;
}

.title-medium {
  font-size: clamp(2rem, 4vw, 3rem);
  font-weight: 700;
  position: relative;
  display: inline-block;
}

.title-medium::after {
  content: '';
  position: absolute;
  bottom: -10px;
  left: 50%;
  transform: translateX(-50%);
  width: 60px;
  height: 4px;
  background: var(--primary);
  border-radius: 2px;
}

.text-lead {
  font-size: clamp(1.1rem, 2vw, 1.25rem);
  color: var(--text-light);
  margin-bottom: 2rem;
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes pulse {
  0% { transform: scale(1) translate(-50%, -50%); box-shadow: 0 0 0 0 rgba(37, 211, 102, 0.7); }
  70% { transform: scale(1.05) translate(-48%, -48%); box-shadow: 0 0 0 15px rgba(37, 211, 102, 0); }
  100% { transform: scale(1) translate(-50%, -50%); box-shadow: 0 0 0 0 rgba(37, 211, 102, 0); }
}

@keyframes float {
  0% { transform: translateY(0px); }
  50% { transform: translateY(-15px); }
  100% { transform: translateY(0px); }
}

.animate-in {
  animation: fadeIn 0.8s ease-out forwards;
}

.stagger-1 { animation-delay: 0.1s; }
.stagger-2 { animation-delay: 0.2s; }
.stagger-3 { animation-delay: 0.3s; }
.stagger-4 { animation-delay: 0.4s; }

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.8rem 2rem;
  border-radius: 50px;
  font-weight: 600;
  font-size: 1.1rem;
  cursor: pointer;
  transition: var(--transition);
  border: none;
  gap: 0.5rem;
  position: relative;
  overflow: hidden;
  z-index: 1;
}

.btn::before {
  content: '';
  position: absolute;
  top: 0; left: 0; width: 100%; height: 100%;
  background: rgba(255, 255, 255, 0.2);
  transform: translateX(-100%);
  transition: transform 0.5s ease;
  z-index: -1;
}

.btn:hover::before {
  transform: translateX(0);
}

.btn:hover {
  transform: translateY(-3px);
  box-shadow: 0 10px 20px -10px var(--accent);
}

.btn:active {
  transform: translateY(1px);
}

.btn-primary {
  background: linear-gradient(135deg, var(--primary), var(--primary-dark));
  color: white;
}

.btn-secondary {
  background: linear-gradient(135deg, var(--secondary), var(--secondary-dark));
  color: white;
}

.btn-outline {
  background: transparent;
  border: 2px solid var(--primary);
  color: var(--primary-dark);
}
.btn-outline:hover {
  background: var(--primary);
  color: white;
}

.container {
  width: 90%;
  max-width: 1200px;
  margin: 0 auto;
}

.page-section {
  padding: 8rem 0 4rem;
  min-height: calc(100vh - 80px);
  display: flex;
  flex-direction: column;
  justify-content: center;
  animation: fadeIn 0.6s ease-out forwards;
}

.navbar {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 80px;
  background: rgba(253, 251, 247, 0.85);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  z-index: 1000;
  border-bottom: 1px solid rgba(120, 53, 15, 0.1);
  transition: var(--transition);
}

.navbar .container {
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo {
  font-family: 'Playfair Display', serif;
  font-size: 1.8rem;
  font-weight: 800;
  color: var(--secondary-dark);
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
}

.logo span {
  color: var(--primary-dark);
}

.nav-links {
  display: flex;
  gap: 2rem;
}

.nav-item {
  font-weight: 500;
  color: var(--text-dark);
  cursor: pointer;
  transition: color 0.3s;
  position: relative;
}

.nav-item::after {
  content: '';
  position: absolute;
  bottom: -4px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--primary);
  transition: width 0.3s ease;
}

.nav-item:hover, .nav-item.active {
  color: var(--primary-dark);
}

.nav-item:hover::after, .nav-item.active::after {
  width: 100%;
}

.mobile-menu-btn {
  display: none;
  background: none;
  border: none;
  font-size: 1.5rem;
  color: var(--accent);
  cursor: pointer;
}

.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: 80px;
  overflow: hidden;
}

.hero-bg {
  position: absolute;
  top: 0; left: 0; width: 100%; height: 100%;
  background-size: cover;
  background-position: center;
  z-index: -2;
  filter: brightness(0.85);
  transform: scale(1.05);
  animation: heroBgZoom 20s infinite alternate;
}

@keyframes heroBgZoom {
  from { transform: scale(1); }
  to { transform: scale(1.1); }
}

.hero-overlay {
  position: absolute;
  top: 0; left: 0; width: 100%; height: 100%;
  background: linear-gradient(to right, rgba(253, 251, 247, 0.95) 0%, rgba(253, 251, 247, 0.7) 50%, rgba(253, 251, 247, 0.2) 100%);
  z-index: -1;
}

.hero-content {
  max-width: 600px;
}

.hero .badge {
  background: rgba(245, 158, 11, 0.2);
  color: var(--primary-dark);
  padding: 0.5rem 1rem;
  border-radius: 50px;
  font-weight: 600;
  display: inline-block;
  margin-bottom: 1.5rem;
  border: 1px solid var(--primary-light);
}

.grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
}

.grid-3 {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.product-card {
  background: var(--bg-card);
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow);
  transition: var(--transition);
  position: relative;
  height: 100%;
  display: flex;
  flex-direction: column;
}

.product-card:hover {
  transform: translateY(-10px);
  box-shadow: var(--shadow-hover);
}

.product-img-wrapper {
  height: 250px;
  overflow: hidden;
  position: relative;
}

.product-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s ease;
}

.product-card:hover .product-img {
  transform: scale(1.1);
}

.product-content {
  padding: 2rem;
  flex-grow: 1;
  display: flex;
  flex-direction: column;
}

.product-price {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--secondary-dark);
  margin: 1rem 0;
}

.tag-list {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
}

.tag {
  background: rgba(22, 163, 74, 0.1);
  color: var(--secondary-dark);
  padding: 0.3rem 0.8rem;
  border-radius: 20px;
  font-size: 0.85rem;
  font-weight: 600;
}

.image-block {
  position: relative;
  border-radius: var(--radius);
  overflow: hidden;
  box-shadow: var(--shadow-hover);
}

.image-block::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: var(--radius);
  box-shadow: inset 0 0 0 2px rgba(255, 255, 255, 0.5);
  pointer-events: none;
}

.image-block .floating-element {
  position: absolute;
  bottom: 20px;
  right: -20px;
  background: white;
  padding: 1.5rem;
  border-radius: var(--radius);
  box-shadow: var(--shadow-hover);
  animation: float 4s ease-in-out infinite;
  z-index: 2;
}

.whatsapp-btn {
  position: fixed;
  bottom: 30px;
  right: 30px;
  width: 60px;
  height: 60px;
  background-color: #25D366;
  color: white;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 35px;
  box-shadow: 2px 2px 10px rgba(0,0,0,0.2);
  z-index: 100;
  transition: var(--transition);
  cursor: pointer;
}

.whatsapp-btn::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background-color: #25D366;
  z-index: -1;
  animation: pulse 2s infinite;
}

.whatsapp-btn:hover {
  transform: scale(1.1);
  background-color: #128C7E;
}

.contact-item {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  margin-bottom: 2rem;
  padding: 1.5rem;
  background: white;
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  transition: var(--transition);
}

.contact-item:hover {
  transform: translateX(10px);
  border-left: 4px solid var(--primary);
}

.contact-icon {
  width: 50px;
  height: 50px;
  background: var(--bg-color);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.5rem;
  color: var(--primary-dark);
}

footer {
  background: var(--accent);
  color: white;
  padding: 4rem 0 2rem;
  text-align: center;
}

.footer-logo {
  font-family: 'Playfair Display', serif;
  font-size: 2rem;
  margin-bottom: 1rem;
}

@media (max-width: 900px) {
  .grid-2 {
    grid-template-columns: 1fr;
    gap: 3rem;
  }
  
  .hero-overlay {
    background: linear-gradient(to bottom, rgba(253, 251, 247, 0.95) 0%, rgba(253, 251, 247, 0.8) 100%);
  }
  
  .navbar .nav-links {
    position: fixed;
    top: 80px;
    left: -100%;
    width: 100%;
    height: calc(100vh - 80px);
    background: var(--bg-color);
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 3rem;
    transition: 0.4s ease-in-out;
    font-size: 1.5rem;
  }
  
  .navbar .nav-links.mobile-active {
    left: 0;
  }
  
  .mobile-menu-btn {
    display: block;
  }
}

    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // Image Constants (Generated Paths)
        const IMAGES = {
          hero: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/we_samuh_hero_1775325801276.png",
          farmer: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/profile_farmer_1775325871832.png",
          honey: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/product_honey_1775325923970.png",
          mushroom: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/product_mushroom_1775325939840.png",
          milk: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/product_milk_1775326122112.png",
          oil: "file:///C:/Users/Baswa%20shankar/.gemini/antigravity/brain/aa56f661-4ae3-401a-96e4-1af47776af0d/product_oil_1775326141996.png"
        };

        // Data
        const PRODUCTS = {
          honey: {
            title: "Pure Honey",
            description: "100% natural, unprocessed honey enriched with pollen and enzymes.",
            benefits: ["Boosts immunity", "Soothes sore throats", "Antioxidant powerhouse"],
            items: [
              { id: 1, name: "Forest Honey", price: "₹650", unit: "kg", tags: ["Raw", "Organic"], img: IMAGES.honey },
              { id: 2, name: "Wild Honey", price: "₹750", unit: "kg", tags: ["Rare", "Unprocessed"], img: IMAGES.honey },
            ]
          },
          mushrooms: {
            title: "Fresh Mushrooms",
            description: "Farm-fresh earthy mushrooms, grown organically in controlled environments.",
            benefits: ["Rich in protein", "Excellent source of Vitamin D", "Low calorie"],
            items: [
              { id: 3, name: "Oyster Mushroom", price: "₹200", unit: "250g", tags: ["Fresh", "Protein-Rich"], img: IMAGES.mushroom },
              { id: 4, name: "Button Mushroom", price: "₹150", unit: "250g", tags: ["Earthy", "Farm-Fresh"], img: IMAGES.mushroom },
            ]
          },
          milk: {
            title: "Farm Dairy",
            description: "Fresh, creamy milk sourced from healthy, naturally grazed local cattle.",
            benefits: ["High calcium content", "Essential vitamins", "Fresh delivery daily"],
            items: [
              { id: 5, name: "A2 Cow Milk", price: "₹90", unit: "L", tags: ["A2 Protein", "Farm Delivery"], img: IMAGES.milk },
              { id: 6, name: "Buffalo Milk", price: "₹80", unit: "L", tags: ["Creamy", "Rich in Calcium"], img: IMAGES.milk },
            ]
          },
          oil: {
            title: "Kusuma Oil (Safflower)",
            description: "Cold-pressed Kusuma oil retaining all natural nutrients and pure flavor.",
            benefits: ["Heart healthy", "Rich in Omega-6", "Great for daily cooking"],
            items: [
              { id: 7, name: "Cold-Pressed Kusuma Oil", price: "₹350", unit: "L", tags: ["Cold-Pressed", "Heart Healthy"], img: IMAGES.oil },
              { id: 8, name: "Wood-Pressed Kusuma Oil", price: "₹450", unit: "L", tags: ["Traditional", "Pure"], img: IMAGES.oil },
            ]
          }
        };

        const FARMERS = [
          { id: 1, name: "Ramesh Babu", location: "Andhra Pradesh", image: IMAGES.farmer, supply: "Honey & Oil", story: "Beekeeping has been in our family for 3 generations. We believe in ethical and natural extraction." },
          { id: 2, name: "Suresh Kumar", location: "Telangana", image: IMAGES.farmer, supply: "Mushrooms", story: "Transitioned from traditional crops to modern organic mushroom farming, supporting local village employment." },
          { id: 3, name: "Lakshmi Devi", location: "Karnataka", image: IMAGES.farmer, supply: "Dairy", story: "Our cows are family. They graze naturally in open fields, ensuring the best quality A2 milk." }
        ];

        // Components
        const Navbar = ({ activeTab, setActiveTab }) => {
          const [mobileOpen, setMobileOpen] = useState(false);

          const navItems = [
            { id: 'home', label: 'Home' },
            { id: 'farmers', label: 'Farmers' },
            { id: 'honey', label: 'Honey' },
            { id: 'mushrooms', label: 'Mushrooms' },
            { id: 'milk', label: 'Milk' },
            { id: 'oil', label: 'Kusuma Oil' },
            { id: 'contact', label: 'Contact' }
          ];

          return (
            <nav className="navbar">
              <div className="container">
                <div className="logo" onClick={() => { setActiveTab('home'); setMobileOpen(false); }}>
                  <i className="fa-solid fa-leaf"></i> We<span>Samuh</span>
                </div>
                
                <div className={`nav-links ${mobileOpen ? 'mobile-active' : ''}`}>
                  {navItems.map(item => (
                    <div 
                      key={item.id} 
                      className={`nav-item ${activeTab === item.id ? 'active' : ''}`}
                      onClick={() => { setActiveTab(item.id); setMobileOpen(false); window.scrollTo(0, 0); }}
                    >
                      {item.label}
                    </div>
                  ))}
                </div>

                <button className="mobile-menu-btn" onClick={() => setMobileOpen(!mobileOpen)}>
                  <i className={`fa-solid ${mobileOpen ? 'fa-times' : 'fa-bars'}`}></i>
                </button>
              </div>
            </nav>
          );
        };

        const Hero = ({ setActiveTab }) => {
          return (
            <section className="hero">
              <div className="hero-bg" style={{ backgroundImage: `url(${IMAGES.hero})` }}></div>
              <div className="hero-overlay"></div>
              <div className="container">
                <div className="hero-content animate-in">
                  <div className="badge"><i className="fa-solid fa-star"></i> 100% Pure & Organic</div>
                  <h1 className="title-large">Direct from Village Farms to Your Table.</h1>
                  <p className="text-lead mt-4">Discover the true taste of nature with We Samuh. Empowering local farmers while delivering the purest Honey, Mushrooms, Milk, and Kusuma Oil.</p>
                  <div style={{ display: 'flex', gap: '1rem', marginTop: '2rem' }}>
                    <button className="btn btn-primary" onClick={() => setActiveTab('honey')}>
                      Shop Products <i className="fa-solid fa-arrow-right"></i>
                    </button>
                    <button className="btn btn-outline" onClick={() => setActiveTab('farmers')}>
                      Meet Farmers
                    </button>
                  </div>
                </div>
              </div>
            </section>
          );
        };

        const ProductCard = ({ product }) => {
          const handleOrder = () => {
            const message = `Hello We Samuh, I want to order ${product.name} at ${product.price}/${product.unit}.`;
            window.open(`https://wa.me/919876543210?text=${encodeURIComponent(message)}`, '_blank');
          };

          return (
            <div className="product-card">
              <div className="product-img-wrapper">
                <img src={product.img} alt={product.name} className="product-img" />
              </div>
              <div className="product-content">
                <div className="tag-list">
                  {product.tags.map((tag, idx) => <span key={idx} className="tag">{tag}</span>)}
                </div>
                <h3>{product.name}</h3>
                <div className="product-price">{product.price} <span style={{ fontSize: '1rem', color: 'gray', fontWeight: 'normal' }}>/ {product.unit}</span></div>
                <button className="btn btn-secondary" style={{ marginTop: 'auto', width: '100%' }} onClick={handleOrder}>
                  <i className="fa-brands fa-whatsapp"></i> Order Now
                </button>
              </div>
            </div>
          );
        };

        const CategorySection = ({ categoryId, categoryData }) => {
          return (
            <section className="page-section container animate-in">
              <div style={{ textAlign: 'center', marginBottom: '4rem' }}>
                <h2 className="title-medium">{categoryData.title}</h2>
                <p className="text-lead" style={{ maxWidth: '700px', margin: '1.5rem auto' }}>{categoryData.description}</p>
                <div style={{ display: 'flex', justifyContent: 'center', gap: '1.5rem', flexWrap: 'wrap' }}>
                  {categoryData.benefits.map((benefit, idx) => (
                    <div key={idx} style={{ display: 'flex', alignItems: 'center', gap: '0.5rem', color: 'var(--secondary-dark)', fontWeight: '600' }}>
                      <i className="fa-solid fa-check-circle"></i> {benefit}
                    </div>
                  ))}
                </div>
              </div>

              <div className="grid-3">
                {categoryData.items.map((item) => (
                  <ProductCard key={item.id} product={item} />
                ))}
              </div>
            </section>
          );
        };

        const FarmersSection = () => {
          return (
            <section className="page-section container animate-in">
              <div style={{ textAlign: 'center', marginBottom: '4rem' }}>
                <h2 className="title-medium">Meet Our Farmers</h2>
                <p className="text-lead">The dedicated hands that nurture nature's gifts.</p>
              </div>

              <div className="grid-3">
                {FARMERS.map((farmer, idx) => (
                  <div key={farmer.id} className="product-card" style={{ animationDelay: `${idx * 0.1}s` }}>
                    <div className="product-img-wrapper">
                      <img src={farmer.image} alt={farmer.name} className="product-img" />
                    </div>
                    <div className="product-content">
                      <div className="tag" style={{ alignSelf: 'flex-start', marginBottom: '1rem' }}>
                        <i className="fa-solid fa-location-dot"></i> {farmer.location}
                      </div>
                      <h3>{farmer.name}</h3>
                      <p style={{ color: 'var(--primary-dark)', fontWeight: '600', marginBottom: '1rem' }}>Supplier of: {farmer.supply}</p>
                      <p style={{ color: 'var(--text-muted)' }}>"{farmer.story}"</p>
                    </div>
                  </div>
                ))}
              </div>
            </section>
          );
        };

        const ContactSection = () => {
          return (
            <section className="page-section container animate-in">
              <div className="grid-2">
                <div>
                  <h2 className="title-medium" style={{ marginBottom: '2rem' }}>Get in Touch</h2>
                  <p className="text-lead">Have questions or want bulk orders? Reach out to us directly.</p>
                  
                  <div className="contact-item">
                    <div className="contact-icon"><i className="fa-solid fa-phone"></i></div>
                    <div>
                      <h4 style={{ marginBottom: '0.2rem' }}>Call Us</h4>
                      <p>+91 98765 43210</p>
                    </div>
                  </div>

                  <div className="contact-item">
                    <div className="contact-icon"><i className="fa-brands fa-whatsapp"></i></div>
                    <div>
                      <h4 style={{ marginBottom: '0.2rem' }}>WhatsApp</h4>
                      <p>+91 98765 43210</p>
                    </div>
                  </div>

                  <div className="contact-item">
                    <div className="contact-icon"><i className="fa-solid fa-envelope"></i></div>
                    <div>
                      <h4 style={{ marginBottom: '0.2rem' }}>Email</h4>
                      <p>hello@wesamuh.com</p>
                    </div>
                  </div>
                </div>

                <div className="image-block" style={{ height: '500px', backgroundColor: '#eee', borderRadius: '16px' }}>
                  <img src={IMAGES.hero} alt="Farm Map Location" style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                  <div className="floating-element">
                    <h4 style={{ margin: 0 }}><i className="fa-solid fa-map-pin" style={{ color: 'var(--primary)' }}></i> Main Office</h4>
                    <p style={{ color: 'var(--text-muted)', fontSize: '0.9rem' }}>Hyderabad, India</p>
                  </div>
                </div>
              </div>
            </section>
          );
        };

        const App = () => {
          const [activeTab, setActiveTab] = useState('home');

          const renderContent = () => {
            switch(activeTab) {
              case 'home': return <Hero setActiveTab={setActiveTab} />;
              case 'farmers': return <FarmersSection />;
              case 'honey': return <CategorySection categoryId="honey" categoryData={PRODUCTS.honey} />;
              case 'mushrooms': return <CategorySection categoryId="mushrooms" categoryData={PRODUCTS.mushrooms} />;
              case 'milk': return <CategorySection categoryId="milk" categoryData={PRODUCTS.milk} />;
              case 'oil': return <CategorySection categoryId="oil" categoryData={PRODUCTS.oil} />;
              case 'contact': return <ContactSection />;
              default: return <Hero setActiveTab={setActiveTab} />;
            }
          };

          return (
            <div>
              <Navbar activeTab={activeTab} setActiveTab={setActiveTab} />
              
              <main style={{ paddingTop: '80px' }}>
                {renderContent()}
              </main>

              <div 
                className="whatsapp-btn" 
                onClick={() => window.open('https://wa.me/919876543210?text=Hello%20We%20Samuh!', '_blank')}
                title="Chat with us on WhatsApp"
              >
                <i className="fa-brands fa-whatsapp"></i>
              </div>

              <footer>
                <div className="container">
                  <div className="footer-logo">
                    We<span style={{ color: 'var(--primary)' }}>Samuh</span>
                  </div>
                  <p style={{ color: 'rgba(255,255,255,0.7)', maxWidth: '500px', margin: '0 auto 2rem' }}>
                    Connecting local hardworking farmers with mindful consumers seeking pure, unadulterated nature's bounty.
                  </p>
                  <p style={{ color: 'rgba(255,255,255,0.5)', fontSize: '0.9rem' }}>
                    &copy; 2026 We Samuh. All Rights Reserved.
                  </p>
                </div>
              </footer>
            </div>
          );
        }

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
// Update current year in footer
document.getElementById('current-year').textContent = new Date().getFullYear();

// WhatsApp Ordering Function
function orderViaWhatsApp(productName) {
    // We Samuh placeholder number
    const phoneNumber = "919876543210"; 
    
    // Create the message
    const message = `Hello We Samuh, I want to order ${productName}.`;
    
    // Encode the url
    const whatsappUrl = `https://wa.me/${phoneNumber}?text=${encodeURIComponent(message)}`;
    
    // Open in new tab
    window.open(whatsappUrl, '_blank');
}

// Navbar Scroll Effect
const navbar = document.getElementById('navbar');
window.addEventListener('scroll', () => {
    if (window.scrollY > 50) {
        navbar.classList.add('scrolled');
    } else {
        navbar.classList.remove('scrolled');
    }
});

// Mobile Navigation Toggle
const hamburger = document.getElementById('hamburger');
const navLinks = document.getElementById('nav-links');
const navItems = document.querySelectorAll('.nav-links a');

hamburger.addEventListener('click', () => {
    navLinks.classList.toggle('active');
    
    // Change icon between bars and times
    const icon = hamburger.querySelector('i');
    if (navLinks.classList.contains('active')) {
        icon.classList.remove('fa-bars');
        icon.classList.add('fa-times');
        hamburger.style.color = '#3E2723'; // Dark color when open
        hamburger.style.zIndex = '1001';
    } else {
        icon.classList.remove('fa-times');
        icon.classList.add('fa-bars');
        // Reset color based on scroll position handled via CSS/logic
        hamburger.style.color = ''; 
    }
});

// Close mobile menu when a link is clicked
navItems.forEach(item => {
    item.addEventListener('click', () => {
        navLinks.classList.remove('active');
        const icon = hamburger.querySelector('i');
        icon.classList.remove('fa-times');
        icon.classList.add('fa-bars');
        hamburger.style.color = '';
    });
});

// Active link highlighting on scroll
const sections = document.querySelectorAll('section');

window.addEventListener('scroll', () => {
    let current = '';
    
    sections.forEach(section => {
        const sectionTop = section.offsetTop;
        const sectionHeight = section.clientHeight;
        
        if (pageYOffset >= (sectionTop - 200)) {
            current = section.getAttribute('id');
        }
    });

    navItems.forEach(item => {
        item.classList.remove('active');
        if (item.getAttribute('href').includes(current) && current !== '') {
            item.classList.add('active');
        }
    });
});
